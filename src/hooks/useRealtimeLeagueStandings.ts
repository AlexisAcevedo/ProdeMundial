import { useLeagueStandings } from './useLeagueStandings';

/**
   * Hook wrapper alias para useLeagueStandings que ya tiene implementado tiempo real
   */
export const useRealtimeLeagueStandings = useLeagueStandings;
